pipeline {
    agent any

    stages {
        stage ('Build'){
          steps {
            echo 'Building'
          }
        }
        stage('Tests') {
            parallel {
                stage('Unit') {
                    steps {
                        sh 'echo Running Unit Tests'
                    }
                }

                stage('Integration') {
                    steps {
                        sh 'echo Running integration test'
                    }
                }
            }
        }
         stage ('Approve') {
steps {
 input message: 'Test Passes. Deploy to production?'
}
}
    }
}
